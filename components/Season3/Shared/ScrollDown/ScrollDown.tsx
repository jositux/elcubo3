import React from 'react';
import styles from './season2.scrollDown.module.scss';

const ScrollDown = () => {

  return (
    <div className={styles.ScrollDown}>
    </div>

  );
};

export default ScrollDown;
